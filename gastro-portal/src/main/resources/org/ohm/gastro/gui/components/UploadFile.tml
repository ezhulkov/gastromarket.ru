<div class="upload-file ${type} ${additionalClass}" data-objectid="${objectId}" data-type="${type}" data-size="${responseSize}">
    <img src="${imageUrl}"/>

    <div class="uploader-button"></div>
</div>